# "" makes the words a string. 
#  the comma means there are 2 elements in the list
# [] square brackets creates a list
# () belong to the choice function, and specify where the parameter gets passed in
# random.choice gives a random, equal chance of picking of any items in the parameter list

# VERSION #1 - You only need to delete the Hashtags to make it work
import random
#cointoss = random.choice(["heads", "tails"])
#print(cointoss)

# VERSION #2 - You only need to delete the Hashtags to make it work
#from random import choice
#cointoss = choice(["heads", "tails"])
#print(cointoss)

#
#import random
number = random.randint(1,10)
print(number)